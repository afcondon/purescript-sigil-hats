workspace:
  extraPackages:
    sigil:
      path: ../purescript-sigil

package:
  name: sigil-hats
  description: Bridge from Sigil layout trees to HATS Tree for declarative composition
  publish:
    version: 0.2.0
    license: MIT
    location:
      githubOwner: afcondon
      githubRepo: purescript-sigil-hats
  dependencies:
    - hylograph-selection: ">=0.3.1 <0.4.0"
    - maybe: ">=6.0.0 <7.0.0"
    - prelude: ">=6.0.2 <7.0.0"
    - sigil: "*"
